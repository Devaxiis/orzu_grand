import 'package:flutter/material.dart';

class KorzinkaScreen extends StatelessWidget {
  const KorzinkaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("KorzinkaScreen"),
      ),
    );
  }
}
