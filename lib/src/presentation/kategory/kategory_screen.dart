import 'package:flutter/material.dart';

class KategoryScreen extends StatelessWidget {
  const KategoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("CategoryScreen"),
      ),
    );
  }
}
