import 'package:flutter/material.dart';

class SevimliScreen extends StatelessWidget {
  const SevimliScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Sevimli Screen"),
      ),
    );
  }
}
